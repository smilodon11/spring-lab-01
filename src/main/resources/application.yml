spring:
  application:
    name: spring-lab

server:
  port: 8080

app:
  owner: Ramazan Iliyas          # put your own details here
  group: IT1CCO-2503IS
  mail:
    from: no-reply@iitu.kz
    retry-count: 3
    timeout: 5s
    enabled: true

logging:
  level:
    kz.iitu: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,env,configprops,conditions